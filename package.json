{
  "name": "notes",
  "version": "0.0.2",
  "engines": {
    "node": ">5.0.0",
    "npm": ">3.0.0"
  },
  "scripts": {
    "build": "cd modules/src/ && shifter --walk --recursive",
    "start": "node ./server.js"
  },
  "dependencies": {
    "express": "3.0.x",
    "shifter": "^0.5.0"
  }
}
